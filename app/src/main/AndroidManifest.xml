<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="quote.broadcast.kamranriyaz.dcsbgsburajourijk">

    <uses-permission android:name="android.permission.INTERNET" />

    <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".MainActivity" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcherbgsbu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherbgsbu"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme"
        android:usesCleartextTraffic="true">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


        <activity android:name=".AboutActivity" />
        <!--activity android:name=".SemAssignmentActivity" /-->
        <!--activity android:name=".LabPracticalActivity" /-->
        <activity android:name=".AssignmentActivity" />
        <!--activity android:name=".ViewResultActivity" /-->
        <activity android:name=".ResultActivity" />
        <activity android:name=".LearningResActivity" />
        <activity android:name=".SyllabusActivity" />
        <activity android:name=".NoticeBoardActivity" />
        <!--activity android:name=".LoginActivity" /-->
        <activity android:name=".MainActivity" />

        <!--activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-->

    </application>

</manifest>